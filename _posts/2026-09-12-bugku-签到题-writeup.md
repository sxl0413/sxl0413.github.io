# Bugku CTF - [滑稽] Writeup

## 一、题目信息
- **平台**：Bugku CTF
- **题目类型**：Web
- **题目分值**：5
- **题目链接**：https://ctf.bugku.com/challenges/detail/id/68.html
- **核心考点**：F12查看源码、前端限制绕过

## 二、解题过程

### 2.1 初始观察
打开题目页面，看到一堆滑稽乱跳


### 2.2 思路推导
根据题目提示和页面特征，判断这是web安全类型的题目。
首先尝试直接寻找flag方法，但并没有交互功能，但快捷键仍然可用，按 `Ctrl+U` 即可查看服务器返回的原始 HTML 文件

### 2.3 关键突破
按 `Ctrl+U` 查看源代码后，在页面底部发现了被 `<!--` 和 `-->` 包裹的注释内容：

<!-- flag{7a1eaf4051fdc38bb2884c4ed3c116dc} -->
### 2.4 最终操作
完整步骤：
如果注释中的 flag 显示为 `&#102;&#108;&#97;&#103;...` 格式，可按以下步骤解码：

1. 按 `Ctrl+U` 查看源代码
2. 寻找flag复制提交
## 三、涉及知识点
- 知识点1： `Ctrl+U` 查看源代码 
|


## 四、总结
本题难度不高，核心在于查看源代码
需要注意的坑是:无
git add .
git commit -m "添加Bugku签到题Writeup"
git push origin master
